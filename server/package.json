{
  "name": "clinilytics-wound-care-api",
  "version": "1.0.0",
  "description": "Self-hosted, HIPAA-aligned API + database for Clinilytics — Wound Care",
  "license": "UNLICENSED",
  "private": true,
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/db.js && node --check src/auth.js && node --check src/crypto.js && node --check src/mailer.js && node --check src/reports.js && node --check src/scheduler.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.14",
    "pg": "^8.12.0"
  }
}
